npm init -y (Para começar o projeto)
Framework utilizado (next)
git init (Inicar o git)
npx git ignore node (ignorar a pasta node modules)
npm install (node modules)
alterar no package.json "test" e alterar para "dev":"next dev"
criar pasta "app" 
dentro da pasta app crie "page.tsx"
dentro adicione :
export default function Page() {
    return <h1>Hello, Next.js!</h1>
}
Usar CSS media querie

Postar no github
Deploy no Vercell